/**
 * Das simulator Package und seine Unter-Package enthält
 * das vollständige Modell (sowohl in der Version für den Editor
 * als auch in der Version für die Simulation) und alle Klassen,
 * die für die Simulation notwendig sind.
 * <br><br>
 * Die wichtigsten Unter-Packages sind:<br>
 * {@link simulator.editmodel}<br>
 * {@link simulator.runmodel}
 * @author Alexander Herzog
 */
package simulator;
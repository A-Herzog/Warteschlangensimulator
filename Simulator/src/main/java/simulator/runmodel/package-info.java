/**
 * Das Laufzeit-Modell, welches durch die Klassen in diesem Package
 * implementiert wird, ist das Modell welches letztendlich
 * simuliert wird. Es wird aus einem Editor-Modell ({@link simulator.editmodel.EditModel})
 * über den {@link simulator.builder.RunModelCreator} erstellt.
 * Die wesentliche Klasse in diesem Package ist:<br>
 * {@link simulator.runmodel.RunModel}
 * @author Alexander Herzog
 * @see simulator.runmodel.RunModel
 * @see simulator.runmodel.RunData
 * @see simulator.runmodel.SimulationData
 */
package simulator.runmodel;